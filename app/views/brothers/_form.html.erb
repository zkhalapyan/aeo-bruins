<%= form_for(@brother, :html => { :multipart => true }) do |f| %>
  <% if @brother.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brother.errors.count, "error") %> prohibited this brother from being saved:</h2>

      <ul>
      <% @brother.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :major %><br>
    <%= f.text_field :major %>
  </div>

  <div class="field">
    <%= f.label :biography %><br>
    <%= f.text_area :biography %>
  </div>

  <%= f.file_field :avatar %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
